graphify-out/

# Local dsh tooling runtime state
.dsh/

# Build artifacts
bin/inferglow-cli
bin/inferglow-cli.exe
cli/bin/
cli/inferglow-cli
server/inferglow-server.exe
server/*.local.exe
sandbox/seatbelt_loader.exe
bin/seatbelt-loader

# Frontend dependencies (any subproject)
node_modules/

# Go build cache / local toolchain (used for reproducible builds)
.gobuild/

# Session planning working files (not part of the repo)
task_plan.md
task_plan_llm_port.md
findings.md
findings_llm_port.md
progress.md
progress_llm_port.md

# Shared test-phase provider config (contains local api_key; template committed)
etc/config.json
